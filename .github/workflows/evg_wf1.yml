name: Hello World Workflow

on: 
  push:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Step_1:Clone+Checkout repository
        uses: actions/checkout@v4

      - name: Step_2:run script # Name:Say Hello
        run: |
          cat 1.txt 
          echo "Hello, GitHub Actions!"
